/**
 * Prompt templates for all LLM calls. Used by API routes and the prompts config page.
 */

export const BUILTIN_PROMPTS: Record<string, { title: string; description: string; category: string; content: string }> = {
  generate_similar: {
    title: '生成相似问',
    description: '根据用户意图生成语义高度相似的表达',
    category: '意图扩展',
    content: `你是一个顶级的中文话术扩展专家。你的任务是根据用户意图，生成严格贴合该意图核心语义的相似表达。

## 最重要的规则（违反将导致生成结果无效）：
1. **核心动作必须完全一致**：提取意图中的核心动词/动作，所有相似问必须围绕同一个动作展开
   - 例如意图是"领券"，核心动作是"领"（领取/抢/获得），绝不能变成"用券"（使用/抵扣/花）
   - 例如意图是"回访"，核心动作是"回访"（打电话回来），绝不能变成"投诉"或"咨询"
   - 例如意图是"退款"，核心动作是"退"（退还/返回），绝不能变成"支付"或"扣款"
2. **核心主体必须保留**：意图中的核心名词（券、订单、客服等）必须出现在每条话术中
3. **语义方向一致**：肯定/否定/疑问的语气方向必须和原意图一致

## 变体方式（在保持核心动作+主体不变的前提下）：
1. 同义词替换核心动词：领→抢→拿→获取→搞到
2. 添加修饰成分：完全领不到→一个都领不到→根本没法领
3. 变化句式：陈述→反问→感叹→省略
4. 添加语气词：啊、吧、嘛、呢、哦
5. 标点变化：有/无标点，不同标点（。？！~…）
6. 长短句变化：2-3字短句到20字长句
7. 主语变化：我→咱→老子→用户（或不带主语）

## 格式要求：
- 每条话术独占一行，不要编号、引号、前缀
- 每条1-30字
- 直接输出，不要任何解释

{intent_context}
用户意图：{intent}
生成条数：{count}条

请严格围绕意图的核心动作生成{count}条表达，不要偏离到其他动作。`,
  },

  generate_regex: {
    title: 'AI 生成正则',
    description: '基于意图和相似问生成话术正则表达式',
    category: '正则生成',
    content: `你是一位精通中文话术正则表达式的顶级专家。请根据意图和相似问列表，生成能精确匹配所有表达的正则表达式。

核心原则：
1. 生成的每一条正则必须能匹配对应的相似问和意图
2. 正则必须覆盖所有列出的相似表达，覆盖率要求100%
3. 用逗号,分隔每一条独立的正则

格式规范：
- 每条正则以 ^ 开头
- 使用 .* 匹配任意中间内容
- 使用 (?:...) 非捕获组处理可选或分支部分
- 标点符号前必须加 .* 来匹配中间可能出现的任何字符，例如：
  ❌ 错误：^.*优惠券.*(?:没用上|用不了)[。.?!]?$
  ✅ 正确：^.*优惠券.*(?:没用上|用不了).*[。.?!]?$
- 考虑中英文标点：[。.?!！？…~] 等
- 考虑口语词：[啊吧嘛呢哦哟诶哈呀哇] 等
- 每条正则尽量独立

请严格按以下JSON格式输出（不要markdown代码块，只输出纯JSON）：
{
  "list": ["正则1", "正则2", "正则3", "..."],
  "coverage": 95
}
其中 coverage 是预估覆盖率（0-100整数），list 是逗号分隔的逐条正则。

{intent_context}
相似问列表（需全部匹配）：
{expressions}

请生成 {count} 条正则表达式，确保覆盖上面所有相似问。`,
  },

  generate_keywords: {
    title: '提取关键词',
    description: '从意图和相似问中提取全面的关键词列表',
    category: '关键词提取',
    content: `你是一个话术关键词扩展专家。请根据用户意图，生成该意图下所有可能的口语化关键词表达。

要求：
1. 关键词必须是真实用户可能说的话术短语
2. 覆盖各种变体：完整表达、省略表达、口语化表达、带语气词表达
3. 每条关键词独占一行，不编号不加引号
4. 生成{count}条关键词
5. 必须包含意图的核心主体词（如"券"、"优惠券"）
6. 考虑各种可能的动词变体、副词搭配、时态变化
7. 包含正面和负面的表达变体
8. 仅输出关键词列表，不要任何解释

{intent_context}
用户意图：{intent}

请严格生成{count}条与意图密切相关的关键词短语。`,
  },

  generate_negative: {
    title: '生成反例测试问',
    description: '生成与意图相关但语义相反或无关的测试问',
    category: '意图扩展',
    content: `你是一个话术测试专家。请根据用户意图，生成反例测试问。

定义：反例测试问是与原意图在主题上有关联（共享核心主体），但语义方向不同或相反的表达。用于测试正则是否会产生误命中。

示例：
- 意图"优惠券无法使用" → 反例："券我领不了"、"券我抢不到"、"券过期了还能用吗"
  （都和"券"相关，但不是"无法使用"的语义）

要求：
1. 必须包含意图的核心主体词（如"券"、"回访"、"客服"）
2. 语义与原意图不同或相反
3. 每条独占一行，不编号不加引号
4. 生成{count}条
5. 仅输出反例表达，不要任何解释

{intent_context}
用户意图：{intent}

请严格生成{count}条与意图相关但语义不同的反例表达。`,
  },

  adapt_regex: {
    title: '适配修正正则',
    description: '根据失败话术修正和扩展正则表达式',
    category: '正则优化',
    content: `你是正则表达式修正专家。当前的话术正则不能匹配某些badcase话术，请修正正则。

核心要求：
1. 必须保证原有的正则不失效（原话术仍然能匹配）
2. 新增或修正正则来覆盖失败的话术
3. 保持精准，不要过度泛化导致误匹配
4. 标点符号前加 .* 提高灵活性
5. 返回修正后的完整正则列表（逐条格式，逗号分隔）

请严格按以下JSON格式输出（不要markdown代码块，只输出纯JSON）：
{
  "adaptedRegex": ["修正后的正则1", "正则2", "新增的正则3", "..."],
  "changes": "简述修改了哪些正则"
}

当前正则列表（必须保留其匹配能力）：
{currentRegex}

失败的话术（需要新增匹配）：
{failedPhrases}`,
  },
};
